<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buyi.dao.AddressDao">
    <insert id="add" parameterType="com.buyi.entity.Address">
        INSERT INTO `address` (`user_id`, `address`, `name`, `telephone`, `postcode`, `create_time`)
        VALUES (#{userId}, #{address}, #{name}, #{telephone}, #{postcode}, #{createTime})
    </insert>

    <select id="queryById" resultType="com.buyi.entity.Address">
        SELECT * FROM `address` WHERE `id` = #{id} AND `delete_status` = #{deleteStatus}
    </select>

    <select id="queryByUserId" resultType="com.buyi.entity.Address">
        SELECT * FROM `address` WHERE `user_id` = #{userId} AND `delete_status` = #{deleteStatus}
    </select>

    <update id="update" parameterType="com.buyi.entity.Address">
        UPDATE `address`
        <set>
            <if test="address != null">
                `address` = #{address},
            </if>
            <if test="name != null">
                `name` = #{name},
            </if>
            <if test="telephone != null">
                `telephone` = #{telephone},
            </if>
            <if test="postcode != null">
                `postcode` = #{postcode},
            </if>
            <if test="deleteStatus != null">
                `delete_status` = #{deleteStatus}
            </if>
        </set>
        WHERE `id` = #{id}
    </update>
</mapper>